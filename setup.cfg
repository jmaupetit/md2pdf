[bdist_wheel]
universal=1

[aliases]
test=pytest

[tool:pytest]
addopts = -vs --cov=md2pdf --cov-report term-missing tests/

[flake8]
exclude =
    .git,
    .tox,
    __pycache__,
    assets,
    build,
    dist,
    venv,
